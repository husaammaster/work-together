# react_app/.env.docker
# The SPA runs in the browser, so it reaches the backend via the host-published
# ports (80 and 8080), not the internal compose service name.
VITE_API_BASE_URL=http://localhost:80
VITE_WS_URL=ws://localhost:8080
